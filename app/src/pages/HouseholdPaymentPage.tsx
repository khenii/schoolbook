// Superseded — household-split payments are now recorded from the unified
// "Record a payment" panel in PaymentsPage.tsx (mode: "Whole household
// (split)"), which also preserves the household-transaction reconciliation
// table this page used to render. /household-payment now redirects to
// /payments (see App.tsx). Left as an empty stub because files can't be
// deleted from this workspace.
export {};
