// Superseded — "Add student" is now the slide-over panel in
// AddStudentPanel.tsx, mounted directly on StudentsPage.tsx, matching the
// 04-students.html mockup (a panel over the roster, not a separate page).
// The /students/new route now redirects to /students?add=1 (see App.tsx).
// This file is intentionally left empty rather than deleted — the sandbox
// this was built in doesn't have delete access to the mounted project
// folder. Safe to delete by hand: `rm app/src/pages/AddStudentPage.tsx`.
export {};
